// Lift-name length limit shared by the Workout and Progress lift validators
// and their input fields.

/** Maximum characters allowed in a lift name across the app. */
export const MAX_LIFT_NAME_LENGTH = 24;
