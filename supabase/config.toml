# Supabase CLI configuration for local development (supabase start / db reset).
# Hosted project: IncrementApp (mcjpdouznyjohviorywg).
project_id = "member-app-ios"

[db]
major_version = 17

[db.seed]
enabled = true
sql_paths = ["./seed.sql"]
